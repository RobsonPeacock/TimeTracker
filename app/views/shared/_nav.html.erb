<div class='logo'>
  <h1>Time Tracker</h1>
</div>

<% if current_user %>
  <ul class="custom-nav nav nav-tabs">
    <li class="nav-item"><%= link_to fa_icon('home'), root_path, class: "nav-link #{active?(root_path)}" %></li>

    <%= render 'shared/employee_nav_items' if employee? %>

    <%= render 'shared/audit_log_tab' if admin? %>

    <li class="nav-item dropdown ml-md-auto">
      <a href="#" data-toggle="dropdown" class="dropdown-toggle nav-link">Options<strong class="caret"></strong></a>
      <ul class="dropdown-menu">
        <%= render 'shared/admin_dropdown' if admin? %>

        <li><%= link_to "Logout", destroy_user_session_path, method: :delete, class: 'dropdown-item' %></li>
      </ul>
    </li>
  </ul>
<% end %>
