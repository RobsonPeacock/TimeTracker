<div class='logo'>
  <h1>Time Tracker</h1>
</div>

<% if current_user %>
  <ul class="custom-nav nav nav-tabs">
    <li class="nav-item"><%= link_to fa_icon('home'), root_path, class: "nav-link #{active?(root_path)}" %></li>
    <li class="nav-item"><%= link_to "Entries", posts_path, class: "nav-link #{active?(posts_path)}" %></li>

    <%= render 'shared/audit_log_tab' if policy(AuditLog).index? %>

    <li class="nav-item"><%= link_to "Request Overtime", new_post_path, class: "nav-link #{active?(new_post_path)}", id: "new_post_from_nav" %></li>
    <li class="nav-item dropdown ml-md-auto">
      <a href="#" data-toggle="dropdown" class="dropdown-toggle nav-link">Options<strong class="caret"></strong></a>
      <ul class="dropdown-menu">

        <!-- TODO -->
        <% if current_user.try(:type) == 'AdminUser' %>
        <li><%= link_to "Admin Dashboard", admin_root_path, class: 'dropdown-item' %></li>
        <li class="divider"></li>
        <% end %>
        <!-- TODO -->

        <li><%= link_to "Logout", destroy_user_session_path, method: :delete, class: 'dropdown-item' %></li>
      </ul>
    </li>
  </ul>
<% end %>
